# ============================================================
# TARA - Telecom Agentic RAG Assistant
# Requirements (Lean Stack for RTX 4050 / 6GB VRAM)
# ============================================================

# --- Core ---
python-dotenv>=1.0.0
pydantic>=2.7.0
pydantic-settings>=2.2.0
loguru>=0.7.0
pyyaml>=6.0.1

# --- LLM & Embeddings ---
langchain>=0.2.0
langchain-community>=0.2.0
langgraph>=0.1.0
sentence-transformers>=3.0.0
# ollama is installed separately (not a pip package)

# --- Vector DB ---
qdrant-client>=1.9.0

# --- PDF Processing ---
PyMuPDF>=1.24.0
pdfplumber>=0.11.0

# --- NLP & NER ---
spacy>=3.7.0

# --- API & UI ---
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
streamlit>=1.35.0

# --- Evaluation ---
ragas>=0.1.0
datasets>=2.19.0

# --- Utilities ---
httpx>=0.27.0
numpy>=1.26.0
pandas>=2.2.0
tqdm>=4.66.0
rich>=13.7.0
